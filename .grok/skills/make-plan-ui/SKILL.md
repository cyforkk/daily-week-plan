---
name: make-plan-ui
description: 日计划工具 UI：去 AI 视觉、主题切换、自定义下拉、手机布局、去多余控件。改样式、主题、下拉、顶栏、弹窗、移动端、按钮布局时使用。
when-to-use: 用户提到界面、颜色、AI 感、夜间模式、下拉、滚动错位、手机显示、现代化、去 AI 化、按钮多余、布局体验。
---

# make_plan UI 技能

## 先读

- `AGENTS.md` UI 硬约束  
- `docs/对话沉淀-需求与教训.md` UI 表  
- `docs/UX精简说明.md`  

## 视觉：去 AI 化（强制）

**禁止**：紫/indigo 主色与渐变按钮、标题渐变字、radial 光晕、强 blur 玻璃、999px 胶囊+重阴影、卡片 hover 抬升、分区彩色装饰点。

**推荐**：纸色底、墨色字与主按钮、4–6px 圆角、细边框、少阴影；夜间中性深灰无霓虹。打印强制浅色。

主题：**按钮**循环 跟随系统 → 浅 → 深，键如 `daily-plan-theme-v1`。

## 自定义下拉（cselect）

1. 选项纯文字（可有灰色说明）；无色点/勾选图标。  
2. 触发器保留 chevron。  
3. 打开时 fixed 贴触发器。  
4. 页面/容器滚动关闭；菜单内滚动不关。  
5. resize 关闭；关闭时清 inline position。  
6. 星期顺序：周一…周日。  

## 布局与手机

- 默认单栏；宽屏可双栏。  
- `minmax(0,1fr)`；宽表 overflow-x 表内滑。  
- **粘性主操作仅手机**；桌面靠顶栏，避免双份噪音。  
- 日/周不对齐时可用 `align-banner` 提示。  

## 体验：砍多余

主路径：概览 → MIT → 时间块 → 护栏 → 复盘关机 → 开启新的一天。  

| 倾向删除/合并 | 保留 |
|---|---|
| 大 Hero/四步流程卡占首屏 | 短标题或无 |
| 同动作多按钮（→明日×3） | 一处主入口+手机底栏 |
| 每区长教学 hint | 底部方法参考 |
| 晨间健康常显 | details 折叠 |
| 顶栏过多锚点 | 审查/参考即可 |

按钮分层：主（开新日）/ 次（保存、示例）/ 危险沉底。

## 验收

- [ ] 无紫渐变/光晕  
- [ ] 主题按钮三态可持久  
- [ ] 下拉纯文字+箭头；滚动不悬空  
- [ ] 手机无整页横滑；底栏可用  
- [ ] 主路径一眼可见，教学不挡写计划  
