# 功能：审查 P0/P1 修复

日期：2026-07-18  
对象：`daily-plan-template.html`  
依据：`docs/审查-代码与业务逻辑-2026-07-18.md`；清空语义用户选 **A**。

## 已修

### P0-1 · 本周重点表单门闩

`bindFormEvents` 中 `goals.weekFocus` ↔ `week.focus`：

- **仅当** `dayBelongsToOpenWeek()` 才跨侧赋值并同步另一输入框  
- 日∉打开周时：只改本侧字段，不污染邻周/日侧  

与 `convergeWeekFocus` / `saveNow` 一致。

### P0-2 · 清空 = 当前查看日（方案 A）

| 之前 | 现在 |
|---|---|
| 强制 `todayISO()`，可能误清今天 | 清空 `state.meta.date`，日期不变 |
| 确认文案笼统「本日」 | 写明具体日期 +「不会清空其它日期」 |
| 未取消 debounce | 先 `cancelScheduledSave()` |

### P1 · 预览 weekStrip

仅当计划日 ∈ 打开周时，才从 `state.week.days` 取「今天主题/焦点」；避免邻周主题张冠李戴。

### P1 · MD / TXT 对齐预览

导出增加「本周每天主题」段（`weekStrip`）；页脚/落款用**计划日**而非系统今天。

### P1 · 防抖竞态

新增 `cancelScheduledSave()`，在 `switchToDate` / `switchToWeek` / 清空 时调用。

## 未做（P2，可后续）

- 明天 Top 3 名实（≥1 vs ≥3）  
- 死字段清理（standup / windDown）  
- `fillThemeWeekPreset` 改名  
- Legacy 整包写入收敛  

## 验收

- [x] 日在本周、周翻邻周 → 改任一侧重点不互写  
- [x] 查看非今天 → 清空 → 清的是该日，今天完好  
- [x] 清空后日期不变、周保留  
- [x] 日∉周时预览无错误「今天主题」  
- [x] MD/TXT 含主题条（日∈周且有内容时）  
